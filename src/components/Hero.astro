<section
  class="w-full flex flex-col gap-5 justify-center items-center px-4 py-32 relative bg-cover bg-center bg-no-repeat"
  style="background-image: url('/assets/background.jpg');"
>
  <!-- Transparent Blur Effect with increased blur -->
  <div class="absolute inset-0 bg-black opacity-40 backdrop-blur-lg"></div> <!-- Increased blur effect -->

  <div class="relative z-10 flex flex-col gap-2 text-center">
    <h1
      class="w-full max-w-xl sm:text-center text-3xl sm:text-5xl md:text-7xl font-medium tracking-tighter text-white"
    >
      Meet India's First socialfi Art MarketPlace
    </h1>
    <p
      class="w-full max-w-xl text-white sm:text-center text-normal sm:text-lg md:text-xl font-light tracking-tighter"
    >
      A platform for artists where user can Create, Connect, Sell and Organise Events.
    </p>
  </div>

  <div class="w-full flex flex-wrap gap-3 items-center justify-center">
    <a
      href="https://x.com/gonzalochale"
      target="_blank"
      rel="noopener noreferrer"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      Download for IOS
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
  </div>

  <div class="w-full flex flex-wrap gap-3 items-center justify-center">
    <a
      href="https://x.com/gonzalochale"
      target="_blank"
      rel="noopener noreferrer"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      Download for Android
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
  </div>

  <div class="w-full flex flex-wrap gap-3 items-center justify-center">
    <a
      href="https://x.com/gonzalochale"
      target="_blank"
      rel="noopener noreferrer"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      Open in Browser
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
  </div>

  <br />
  <p
    class="w-full max-w-xl text-white sm:text-center text-sm sm:text-base md:text-lg font-light tracking-tight"
  >
    Create your own painting authentic with NFC, Connect with other Artists, Sell and Bid your Authentic Paintings, Organise Events and workshops, Keep tracking your presence online.
  </p>

  <h1 class="w-full max-w-xl sm:text-center text-2xl sm:text-3xl md:text-4xl font-medium tracking-tighter text-white flex items-center justify-center gap-2">
    <span>Powered by Solana</span>
    <img
      src="/assets/solana.png"
      class="h-8 sm:h-10 md:h-12"
    />
  </h1>
</section>

<style>
  @keyframes heroIn {
    0% {
      opacity: 0;
      transform: translateY(0.5rem);
    }
    60% {
      opacity: 1;
      transform: translateY(-0.05rem);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  h1 {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) forwards;
  }

  p {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.3s forwards;
  }

  a {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.6s forwards;
  }
</style>
