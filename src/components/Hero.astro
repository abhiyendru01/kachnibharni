<section
  class="w-full flex flex-col gap-5 justify-center items-center px-4 py-32 pattern"
>
  <div class="flex flex-col gap-2">
    <h1
      class="w-full max-w-xl sm:text-center text-3xl sm:text-5xl md:text-7xl font-medium tracking-tighter bg-clip-text text-transparent bg-gradient-to-r from-foreground via-muted-foreground to-foreground"
    >
      Always building things for the web.
    </h1>
    <h2
      class="w-full max-w-xl text-muted-foreground sm:text-center text-normal sm:text-lg md:text-xl font-light tracking-tighter"
    >
      This is a simple portfolio template built with Astro and Tailwind CSS.
    </h2>
  </div>
  <div
    class="w-full flex flex-wrap gap-3 items-center justify-start sm:justify-center"
  >
    <a
      href="https://x.com/gonzalochale"
      target="_blank"
      rel="noopener noreferrer"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      Connect with me
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
  </div>
</section>

<style>
  @keyframes heroIn {
    0% {
      opacity: 0;
      transform: translateY(0.5rem);
    }
    60% {
      opacity: 1;
      transform: translateY(-0.05rem);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  h1 {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) forwards;
  }
  h2 {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.3s forwards;
  }
  a {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.6s forwards;
  }

  .pattern {
    background-color: #ffffff;
    background-image: radial-gradient(
        circle at top center,
        #f5fdff 100px,
        transparent
      ),
      repeating-radial-gradient(
        circle at top center,
        #ffffff,
        #ffffff,
        200px,
        transparent 0,
        #f1f1f1 201px
      );
    background-blend-mode: multiply;
  }
</style>
